# E-İmza Build Ledger

Authoritative specification: [`docs/EIMZA_INTEGRATION_PLAN.md`](../EIMZA_INTEGRATION_PLAN.md).  
This ledger records implementation evidence. A phase is not complete merely because code exists.

## State

| Phase | Title | Status | Evidence / blockers |
|---:|---|---|---|
| 0 | Intake, provenance, boundaries | DONE | Owned module, deterministic snapshot digest, MIT license, selective import record, exclusions, threat/privacy/interoperability/pilot docs, CI, dependency boundary, clean govulncheck, and generated CycloneDX SBOM evidence completed. |
| 1 | Core remediation and conformance | IN PROGRESS | Constrained RSA/SHA-256 B-B signer, corrected SigningCertificateV2 encoding/binding, strict ByteRange checks, exact key/certificate match, controlled PDF corpus, and local negative tests implemented. Two independent validators, trust/revocation policy, fuzzing, and broader corpus remain pending. |
| 2 | Signerd + SoftHSM vertical slice | IN PROGRESS | Plan Phases 2A, 2B, 2C, 2D (core) done. 2A: durable journal, migrations, extended state model, idempotent jobs, CAS transitions, events, recovery, safe errors, contracts, readiness. 2B: exact no-fallback PKCS#11 selection, module digest/ownership verification, cert/key proof, credential registry with shared physical-token queue key, `yargi-signerctl`, cgo `miekg/pkcs11` adapter behind `-tags pkcs11`, safe `GET /v1/credentials`. 2C: signed ES256 PIN challenge + ECDH-ES/A256GCM JWE envelope, one-time durable consumption + `AUTHORIZATION_CONSUMED`, queue-key serialization with in-memory PIN handoff, internal job endpoints. 2D: just-in-time artifact capability broker, supervisor `PlanAuthorization`, token-worker pipeline (fetch+rehash → plan → authorize → one C_Sign at the `AUTHORIZATION_CONSUMED→SIGNING` CAS → finalize → local verify → store → QUARANTINED), producing a real PAdES B-B output through the tested eimza-go `crypto.Signer` seam. Deferred: OS-process sandboxing of the format/validator workers, live SoftHSM, daemon coordinator wiring, and the second independent validator (Phase 1A/1B). Output remains QUARANTINED until the Phase 1B gate closes. |
| 3 | FastAPI persistence and bridge | TODO | Alembic/artifacts/models/security/routes intentionally not started before core conformance. |
| 4 | React signing experience | TODO | Intentionally not started. |
| 5 | One-owner real-token pilot | BLOCKED | Requires owner consent, qualified SoftHSM path, exact token inventory, and approved protocol. |
| 6 | Trusted timestamp / PAdES B-T | BLOCKED | Timestamp package and KamuSM/TSA provenance not qualified. |
| 7 | Production hardening | BLOCKED | Legal, operational, retention, interoperability, deployment, and incident gates incomplete. |

## Completed foundation

- [x] Created `feat/eimza-integration` from `main`.
- [x] Created an owned `signing-service` Go module pinned to patched Go 1.26.5; govulncheck evidence required raising the initial 1.26.3 pin.
- [x] Kept the service disabled by default and rejected SoftHSM in production.
- [x] Implemented only private Unix-socket transport; unimplemented network transport fails closed.
- [x] Fixed creation policy to PAdES Baseline B-B and RSA PKCS#1 v1.5/SHA-256.
- [x] Added signing states, legal transitions, terminal immutability, cancellation boundary, and `OUTCOME_UNKNOWN` no-requeue behavior.
- [x] Added bounded per-token workers, same-token serialization, cross-token parallelism, and an enqueue/close barrier that avoids channel-close races and global-lock blocking.
- [x] Added safe `GET /v1/health`; no signing, arbitrary digest/path/URL/module, PIN, or credential endpoint exists. Runtime checks confirm socket mode `0600`, graceful removal, and production parent-directory ownership/permission/symlink checks.
- [x] Added threat-model, retention/privacy, interoperability, and pilot drafts.
- [x] Recorded the claimed upstream URL, unknown commit status, deterministic 203-file snapshot digest, copied files, exclusions, and local patches.
- [x] Preserved the MIT license and confirmed the current runtime dependency graph contains no external Go modules.
- [x] Added a constrained RSA/SHA-256 detached CMS signer with mandatory and certificate-bound `SigningCertificateV2`.
- [x] Added controlled-corpus PAdES B-B creation that preserves the actual catalog and trailer identity metadata while rejecting forms, prior signatures, incremental/hybrid updates, encryption, and xref-stream signing.
- [x] Added lexical PDF-name/dictionary parsing, strict classic/xref-stream failure handling, bounded verification input, strict ByteRange validation, and local cryptographic/profile verification with explicit non-independent status.
- [x] Required outer CMS SHA-256/`id-data` declarations and a well-formed signed `signingTime` within the certificate validity interval.
- [x] Added Go formatting, unit/race test, vet, vulnerability-scan, and SBOM CI scaffolding.

## Phase 2A — persistence, configuration, and internal contracts

- [x] Pinned pure-Go SQLite driver `modernc.org/sqlite v1.51.0` (no cgo) and added `gopkg.in/yaml.v3 v3.0.1` for the module allowlist; runtime graph now includes external modules and the CycloneDX SBOM lists 11 components.
- [x] Signer journal (`internal/persistence`) opens WAL mode with `busy_timeout`, `foreign_keys`, and `synchronous(FULL)`, enforces a 0700 parent directory and 0600 database file, and runs migrations on open.
- [x] Forward-only, checksum-verified migrations create `credentials`, `jobs`, `job_events`, `commands`, `pin_challenges`, `plan_authorizations`, `validation_evidence`, `administrative_events`, and `schema_migrations`; `job_events`, `validation_evidence`, and `administrative_events` are append-only via DB triggers; no PIN/JWE/private-key column exists anywhere.
- [x] Extended the state model with non-terminal `AUTHORIZATION_CONSUMED` and terminal `APPROVAL_EXPIRED`; `SIGNING` is reachable only via `AUTHORIZATION_CONSUMED`; cancellation is refused once authorization is consumed; `QUEUED`/`WAITING_FOR_TOKEN`/`PIN_REQUIRED` may expire their approval.
- [x] Durable job service (`internal/jobs`): idempotent creation keyed by command ID + canonical payload SHA-256 (same payload returns the original job; a different payload returns a conflict), credential existence/enablement and certificate-fingerprint checks, policy-version/profile/algorithm enforcement, queue key derived from the credential (never the request), transactional version-CAS transitions, monotonic per-job events, and command reconciliation.
- [x] Crash-recovery classification: a durable `SIGNING` claim becomes `OUTCOME_UNKNOWN`; a stuck `AUTHORIZATION_CONSUMED` becomes `FAILED_PRE_SIGN` (`AUTHORIZATION_LOST_BEFORE_SIGNING`), discarding the consumed authorization; other non-terminal jobs are preserved; nothing is auto-replayed; recovery is idempotent.
- [x] Safe error taxonomy (`internal/errcodes`, plan §14) with a known-code guard; transitions and events refuse unmapped failure codes.
- [x] Strict internal contracts (`internal/api`): job/credential/authorize DTOs, a JSON decoder that rejects unknown fields and trailing content, and safe `/v1/health` readiness (`ready`, `database`) that exposes no credential inventory.
- [x] Daemon startup opens the journal, loads the module allowlist when configured, reconciles jobs, and installs a DB-backed readiness probe; all signing routes remain absent behind the disabled default.

## Phase 2B — exact SoftHSM credential enrollment

- [x] Abstracted PKCS#11 behind a `token.Backend` interface so selection/proof are testable without hardware; the real cgo `github.com/miekg/pkcs11 v1.1.2` adapter lives behind `//go:build pkcs11` with a pure-Go stub for the default build. `CGO_ENABLED=0 go build ./...` stays pure-Go; `go build -tags pkcs11 ./...` compiles the adapter.
- [x] Exact, no-fallback selection (`token.Resolve`): the requested slot, certificate fingerprint, and CKA_ID must each match exactly one object; zero matches, multiple matches, an absent slot, or a moved/changed token serial all fail closed. There is no first-slot/first-cert/first-key fallback.
- [x] Module verification (`token.VerifyModule`): canonical symlink-free path, non-group/other-writable permissions, expected ownership, exact SHA-256 against the allowlist, and mode allowlist.
- [x] Certificate/private-key proof (`token.VerifyKeyMatch`): public-attribute cross-check without a PIN when the token exposes it, otherwise a single PIN-bearing RSA PKCS#1 v1.5/SHA-256 signature proof over a random nonce. RSA-only, matching the fixed policy.
- [x] Credential registry (`internal/credentials`): enrollment resolves + proves + persists the exact tuple; rejects duplicate certificates and out-of-validity certificates; derives a stable, opaque `physical_token_queue_key` from the token serial so multiple credentials on one physical token share a queue; enable/disable and `verify` re-probe; `remove` is refused while unreconciled (non-terminal) jobs reference the credential and preserves job evidence.
- [x] `yargi-signerctl` commands: `modules list`, `credentials probe|enroll|list|verify|disable|enable|remove`. The PIN is read only from a no-echo interactive TTY (rejected on a pipe/file), never via flag/env/file; output masks token serials and subjects unless `--local-details`; `remove` requires an interactive `yes` confirmation.
- [x] Safe `GET /v1/credentials` inventory returns only public metadata (full certificate fingerprint, masked subject `E*** A***`, issuer, serial suffix, validity, key type/bits, mode, status) and never module paths/digests, slot IDs, full token serials, labels, or CKA_ID.

## Phase 2C — durable job API, queue, events, and PIN challenge

- [x] `internal/pin`: signed ES256 challenge binding job/request IDs, input hash, certificate fingerprint, policy version, nonce, expiry, key ID, allowed JWE `alg`/`enc`, and the ephemeral recipient JWK + RFC 7638 thumbprint. The PIN envelope is a compact JWE restricted to the single ECDH-ES + A256GCM profile with a per-challenge ephemeral recipient key; decryption enforces the profile and the protected-header `kid` = challenge ID. Built on `github.com/go-jose/go-jose/v4 v4.1.4`.
- [x] Durable one-time consumption (`jobs.ConsumeChallenge`): a single transaction marks the challenge consumed and moves the job `PIN_REQUIRED → AUTHORIZATION_CONSUMED`; a repeat returns already-consumed and performs no second transition. No PIN or ciphertext column exists; the ephemeral private key is held in memory only.
- [x] `internal/signer` coordinator: enqueues jobs onto the token queue keyed **only** by `physical_token_queue_key`, reaches `PIN_REQUIRED`, issues the challenge, and blocks the token's queue slot until a decrypted PIN arrives via a bounded in-memory handoff — giving same-token serialization and cross-token parallelism. It hands the PIN to an injected `Operation` and never signs; the token login+sign Operation is Phase 2D.
- [x] Internal job endpoints wired to the coordinator: `POST /v1/jobs` (idempotent create + enqueue, `Idempotency-Key` must equal `command_id`), `GET /v1/commands/{id}`, `GET /v1/jobs/{id}`, `GET /v1/jobs/{id}/events?after=`, `GET /v1/jobs/{id}/pin-challenge`, `POST /v1/jobs/{id}/authorize`, `POST /v1/jobs/{id}/cancel`, with the safe error taxonomy and cancellation refused once authorization is consumed.

## Phase 2D — prepare, sign, finalize, verify, and artifact capabilities

- [x] `internal/artifacts`: in-process artifact store + just-in-time capability broker. Read capabilities are minted only after the broker verifies the artifact hash and size; write capabilities are bounded by a max size. Capabilities are one-use, method-scoped, artifact/job-scoped, and expiring; reuse, method mismatch, forged token, expiry, and size overflow all fail closed. Phase 3A replaces the transport without changing the contract.
- [x] `internal/planauth`: immutable `SigningPlan` with a canonical hash and a supervisor `PlanAuthorization` MAC-authenticated with a supervisor-only key. The supervisor validates the candidate plan against the immutable job manifest (job/command/version, input artifact/hash/size, certificate fingerprint, profile/algorithm/mechanism, policy) and an allowlisted format-engine version; it issues a one-use authorization bound to the plan hash and nonce. Verification is constant-time and consumes the nonce once; forged, replayed, expired, wrong-job, wrong-input, wrong-engine, and wrong-plan-hash authorizations all fail closed.
- [x] `internal/pades` wraps the quarantined eimza-go `SignBaselineBB`/`VerifyCryptographic` so API/worker code never imports `third_party` directly; the token participates only through the `crypto.Signer` seam.
- [x] Token-worker pipeline (`internal/signer` `TokenOperation`): resolve the exact credential, fetch the input through a read capability, rehash and re-verify hash/size, build the plan, obtain a supervisor `PlanAuthorization`, and sign. The `crypto.Signer` implementation consumes the authorization one-use and performs the durable `AUTHORIZATION_CONSUMED → SIGNING` compare-and-swap immediately before the single `C_Sign`, then finalizes, runs local verification, stores the output through a one-use write capability, records the output hash and normalized evidence, and terminates in `QUARANTINED`. A failure before the token call → `FAILED_PRE_SIGN`; an ambiguous token error → `OUTCOME_UNKNOWN`; a post-sign finalization/verification failure → `FAILED_POST_SIGN`; none ever re-sign.
- [x] `internal/validation`: `Local` structural/cryptographic verification and a digest-pinned, timeout-bounded `ExternalValidator` that fails closed on a digest mismatch or missing executable and normalizes outcomes to `valid | invalid | indeterminate`.

## Verification log

- 2026-07-18: `go -C signing-service test ./...` passed.
- 2026-07-18: `go -C signing-service test -race ./...` passed.
- 2026-07-18: `go -C signing-service vet ./...` passed.
- 2026-07-18: govulncheck v1.6.0 initially found three reachable Go 1.26.3 standard-library vulnerabilities; the module minimum was raised to Go 1.26.5 and the scan then reported `No vulnerabilities found`.
- 2026-07-18: CycloneDX generator v1.10.0 produced a CycloneDX 1.6 SBOM with zero external module components; the generated file is an ignored/CI artifact.
- 2026-07-18: selective fork tests passed for mandatory `SigningCertificateV2`, certificate/key mismatch, expiry, signed-content mutation, PDF preflight, and negative ByteRange handling.
- 2026-07-18: `go list -deps` showed no non-standard dependency outside the owned signing-service module.
- 2026-07-18: Poppler `pdfsig` 26.04.0 independently parsed the controlled PDF and reported `Signature Validation: Signature is Valid`, SHA-256, `ETSI.CAdES.detached`, and total-document coverage. Its first run exposed bad incremental xref gaps and a missing widget; both were fixed and the warnings disappeared.
- 2026-07-18: LibreSSL/OpenSSL `cms -verify` 3.3.6 independently verified the extracted detached CMS and content.
- 2026-07-19: After direct manual review hardening, formatting, all unit tests, race tests, vet, dependency-boundary checks, creation-policy exclusions, govulncheck v1.6.0, and CycloneDX v1.10.0 SBOM generation passed again.
- 2026-07-19: An isolated external Go module consumed the public CAdES/PAdES packages, produced internally verified output, and recorded signed-PDF SHA-256 `81044f556c0fdf733a09f0e34006657e08d487e918141e60f754237792274f97` for that ephemeral run.
- 2026-07-19: Poppler `pdfsig` 26.04.0 again reported SHA-256, `ETSI.CAdES.detached`, total-document coverage, and `Signature Validation: Signature is Valid`; LibreSSL 3.3.6 again reported `Verification successful`. The self-signed issuer remained intentionally unknown.
- 2026-07-19: Runtime checks confirmed disabled-by-default exit, production SoftHSM rejection, Unix-socket-only health metadata, `POST /v1/health` = `405`, `/v1/jobs` = `404`, socket mode `0600`, and socket removal on graceful shutdown.
- 2026-07-19: FastAPI OpenAPI contained 28 paths and no signing path; importing the app created no signer socket.
- 2026-07-19: Phase 2A — `go -C signing-service test ./...`, `go -C signing-service test -race ./internal/... ./test/...`, `go -C signing-service vet ./...`, and `gofmt` all passed, covering the extended state machine, module allowlist, journal permissions/migrations/append-only triggers, idempotent job creation and conflicts, credential/policy/certificate rejection, version-CAS and event monotonicity, and crash-recovery classification.
- 2026-07-19: Phase 2A — a genuine on-disk close/reopen integration test preserved a job and its four events across restart, and an ambiguous `SIGNING` claim reconciled to `OUTCOME_UNKNOWN` with no replay.
- 2026-07-19: Phase 2A — enabled-mode end-to-end smoke: the daemon opened the private socket, `GET /v1/health` returned `{ready:true, database:"ok"}`, the journal was created at mode 0600 inside a 0700 directory, recovery ran on startup, and a restart reran recovery cleanly.
- 2026-07-19: Phase 2A — govulncheck v1.6.0 reported no reachable vulnerabilities after adding `modernc.org/sqlite`/`yaml.v3`; one require-only, unreachable advisory (GO-2026-5024, a Windows-only `golang.org/x/sys` symbol) remains. CycloneDX v1.10.0 regenerated the SBOM (CycloneDX 1.6, 11 external components); the file stays an ignored/CI artifact.
- 2026-07-19: Phase 2B — hermetic tests over an in-memory PKCS#11 backend covered exact match, no first-slot/cert/key fallback, absent slot, moved token serial, ambiguous certificate, ambiguous key, missing key, module digest/symlink/permission/mode failures, public-attribute and PIN-signature proof, certificate/key mismatch, wrong-PIN login failure, duplicate certificate, expired certificate, shared physical-token queue key, disable/inventory masking, and remove-blocked-by-unreconciled-job. A journal-wide sentinel scan confirmed an enrollment PIN appears in no table after the proof completes.
- 2026-07-19: Phase 2B — `CGO_ENABLED=0 go build ./...` (pure-Go) and `go build -tags pkcs11 ./...` (cgo `miekg/pkcs11`) both compiled; `go test -race`, `vet`, and `gofmt` passed; govulncheck v1.6.0 reported no reachable vulnerabilities in both the default and `-tags pkcs11` configurations. `yargi-signerctl modules list` and `credentials list` ran; `credentials probe` failed closed on an absent module path. SBOM regenerated (13 components, incl. `miekg/pkcs11` and `x/term`). The live SoftHSM login/proof test remains pending on a SoftHSM-provisioned host.
- 2026-07-19: Phase 2C — PIN-flow tests covered JWS manifest binding, envelope round-trip, and rejection of a wrong challenge `kid`, a disallowed content-encryption, a wrong recipient key, and a malformed envelope. Coordinator tests proved same-token serialization (a second job on a shared queue key stays `QUEUED` while the first holds the token), cross-token parallelism, one-time authorization (repeat returns `already_consumed`, PIN handed to the operation exactly once), and a journal-wide sentinel scan finding the submitted PIN in no table. HTTP tests covered create idempotency/conflict, job/events reads, the pin-challenge→authorize round-trip, and the cancellation boundary. `go test -race`, `vet`, `gofmt`, and govulncheck v1.6.0 (0 reachable) all passed; SBOM regenerated (14 components, incl. `go-jose/v4`).
- 2026-07-19: Phase 2D — the full vertical slice produced a real PAdES B-B output through a software-key token backend: the job reached `QUARANTINED`, the signed PDF was stored through a one-use write capability, the output hash/size and validation evidence were recorded, and the token signed exactly once. Negative tests proved a wrong input hash fails before any token call (`FAILED_PRE_SIGN`, zero signatures) and a mismatched signing key fails after exactly one signature (`FAILED_POST_SIGN`, no retry). Broker tests covered one-use, method, token, expiry, and size-bound enforcement; plan-authorization tests covered forgery, replay, expiry, manifest mismatch, rogue engine, and plan-hash mismatch; validator tests covered digest-pinned fail-closed and outcome normalization. `go test -race`, `vet`, `gofmt`, both build tags, and govulncheck v1.6.0 (0 reachable) all passed; dependencies unchanged.
- Independent PAdES validation: **one full validator pass only; second full PAdES validator and trusted-chain evidence remain pending, so output stays quarantined**.

## Explicit non-claims

The current branch can produce a local software-key signature over a deliberately narrow test PDF corpus, solely for conformance development. It does not access hardware, accept PINs, provide a SoftHSM vertical slice, establish certificate trust/revocation/qualification, pass independent validators, or change the FastAPI/React application. It must not be represented as production-ready, qualified, timestamped, LT/LTA-capable, or UYAP-integrated.
